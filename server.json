{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.u-c4n/autocad-mcp",
  "title": "AutoCAD MCP Pro",
  "description": "AutoCAD for AI agents, live or headless: drafting, P&ID, mechanical parts, architecture, takeoffs.",
  "repository": {
    "url": "https://github.com/U-C4N/Autocad-MCP",
    "source": "github"
  },
  "version": "1.6.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "autocad-mcp-pro",
      "version": "1.6.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "AUTOCAD_MCP_BACKEND",
          "description": "Backend selection: auto (default), com (live AutoCAD, Windows) or ezdxf (headless DXF).",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "ALLOWED_PATHS",
          "description": "Comma-separated absolute paths the server may read and write. Empty means no allowlist is enforced at all - traversal and system-directory checks still run, but nothing scopes the filesystem. Set this.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "TOOL_PROFILE",
          "description": "Exposed tool profile: full (default) or lean.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "TOOL_PACKS",
          "description": "Vertical packs to advertise: all (default) or a comma list of core, pid, settings, mech, arch, plant; core is always on.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "DISCOVERY_MODE",
          "description": "Tool discovery: off (default) advertises the full catalog; search replaces it with search_tools plus a call_tool proxy.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
